import { Component } from '@angular/core';

@Component({
    selector: 'project-root',
    templateUrl: './app.component.html'
})
export class AppComponent {
    pageTitle: string = 'Forms Learning with Angular';
}